---
import Layout from '../layouts/Layout.astro';
import { getCollection } from 'astro:content';

// 获取所有新闻
const allNews = await getCollection('news');

// 按日期排序（最新的在前）
const sortedNews = allNews.sort((a, b) => 
  new Date(b.data.date).getTime() - new Date(a.data.date).getTime()
);

// 获取最新的6篇新闻
const latestNews = sortedNews.slice(0, 6);
---

<Layout title="UMEP Lab - Home">
    <!-- Main Content Container -->
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        
        <!-- Hero Section -->
        <div class="bg-gradient-to-br from-slate-50 via-white to-blue-50 rounded-3xl py-12 lg:py-20 mb-12 lg:mb-16 -mx-4 sm:-mx-6 lg:-mx-8 px-4 sm:px-6 lg:px-8">
            <!-- Research Showcase Carousel with Hero -->
            <section class="mb-8">
                <div class="flex flex-col xl:flex-row items-center xl:items-start gap-8 xl:gap-12">
                    <!-- Left side: Research Image -->
                    <div class="w-full max-w-md xl:w-96 xl:h-96 xl:flex-shrink-0">
                        <img 
                            src="banner.png" 
                            alt="UMEP Lab Logo" 
                            class="w-full h-64 xl:h-full object-cover rounded-2xl shadow-2xl hover:shadow-3xl transition-shadow duration-300"
                        />
                    </div>
                    
                    <!-- Right side: Carousel -->
                    <div class="flex-1 relative overflow-hidden h-64 xl:h-96 rounded-2xl w-full" 
                         style="background-image: url('/hero.png'); background-size: cover; background-position: center; background-repeat: no-repeat;">
                        <!-- Background overlay -->
                        <div class="absolute inset-0 bg-black/10 rounded-2xl"></div>

                    <div class="research-carousel flex transition-transform duration-500 ease-in-out relative z-10">
                        <!-- Slide 0: UMEP Lab Hero -->
                        <div class="research-slide w-full flex-shrink-0">
                            <div class="relative h-64 xl:h-96 bg-black/10 rounded-xl overflow-hidden backdrop-blur-sm">
                                <div class="absolute inset-0 flex items-center justify-center">
                                    <div class="text-center text-white px-4 xl:px-8">
                                        <h1 class="text-2xl xl:text-4xl font-serif font-bold mb-2 xl:mb-4 drop-shadow-lg">UMEP Lab</h1>
                                        <h2 class="text-sm xl:text-lg mb-4 xl:mb-6 opacity-90 drop-shadow-md">Urban Mobility and Environmental Perception Laboratory</h2>
                                        <p class="text-xs xl:text-sm opacity-80 max-w-xs xl:max-w-md mx-auto leading-relaxed drop-shadow-md mb-4 xl:mb-6">
                                            Advancing our understanding of urban mobility, environmental perception, and sustainable city development through innovative research and cutting-edge technology.
                                        </p>
                                        <a href="/publications" class="inline-flex items-center px-4 xl:px-6 py-2 xl:py-3 bg-white/20 hover:bg-white/30 text-white rounded-lg hover:rounded-xl transition-all duration-300 font-semibold text-sm xl:text-base backdrop-blur-sm border border-white/20 hover:border-white/40 shadow-lg hover:shadow-xl transform hover:-translate-y-1">
                                            Explore Our Research
                                            <svg class="w-4 h-4 xl:w-5 xl:h-5 ml-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"></path>
                                            </svg>
                                        </a>
                                    </div>
                                </div>
                            </div>
                        </div>

                        <!-- Research Slide 1: LLM-driven Agent-Based Mobility Simulation -->
                        <div class="research-slide w-full flex-shrink-0">
                            <div class="relative h-64 xl:h-96 bg-black/10 rounded-xl overflow-hidden backdrop-blur-sm">
                                <div class="absolute inset-0 flex items-center justify-center">
                                    <div class="text-center text-white px-4 xl:px-8">
                                        <div class="w-12 h-12 xl:w-16 xl:h-16 bg-white/1 rounded-full flex items-center justify-center mb-4 xl:mb-6 mx-auto">
                                            <svg class="w-6 h-6 xl:w-8 xl:h-8" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 9l4 4 4-4M12 13V4m-7 9a7 7 0 1014 0 7 7 0 00-14 0z"></path>
                                            </svg>
                                        </div>
                                        <h3 class="text-lg xl:text-2xl font-serif font-bold mb-2 xl:mb-4 drop-shadow-lg">LLM-driven Agent-Based Mobility Simulation</h3>
                                        <p class="text-sm xl:text-base opacity-90 max-w-sm xl:max-w-lg mx-auto drop-shadow-md mb-3 xl:mb-4">Hierarchical LLM agents for activity scheduling, mode/route choice, and dynamic replanning under context and policies.</p>
                                        <a href="/publications" class="inline-flex items-center px-3 xl:px-4 py-1.5 xl:py-2 bg-white/20 hover:bg-white/30 text-white rounded-lg transition-all duration-300 font-medium text-xs xl:text-sm backdrop-blur-sm border border-white/20 hover:border-white/40">
                                            Learn More
                                            <svg class="w-3 h-3 xl:w-4 xl:h-4 ml-1.5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"></path>
                                            </svg>
                                        </a>
                                    </div>
                                </div>
                            </div>
                        </div>

                        <!-- Research Slide 2: Urban Environment Perception Modeling -->
                        <div class="research-slide w-full flex-shrink-0">
                            <div class="relative h-64 xl:h-96 bg-black/10 rounded-xl overflow-hidden backdrop-blur-sm">
                                <div class="absolute inset-0 flex items-center justify-center">
                                    <div class="text-center text-white px-4 xl:px-8">
                                        <div class="w-12 h-12 xl:w-16 xl:h-16 bg-white/20 rounded-full flex items-center justify-center mb-4 xl:mb-6 mx-auto">
                                            <svg class="w-6 h-6 xl:w-8 xl:h-8" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 7h7l2 3h9M5 21h14a2 2 0 002-2V8a2 2 0 00-2-2H5a2 2 0 00-2 2v11a2 2 0 002 2z"></path>
                                            </svg>
                                        </div>
                                        <h3 class="text-lg xl:text-2xl font-serif font-bold mb-2 xl:mb-4 drop-shadow-lg">Urban Environment Perception Modeling</h3>
                                        <p class="text-sm xl:text-base opacity-90 max-w-sm xl:max-w-lg mx-auto drop-shadow-md mb-3 xl:mb-4">Fusing street-view imagery, remote sensing, and POI to learn semantic attributes of the built environment.</p>
                                        <a href="/publications" class="inline-flex items-center px-3 xl:px-4 py-1.5 xl:py-2 bg-white/20 hover:bg-white/30 text-white rounded-lg transition-all duration-300 font-medium text-xs xl:text-sm backdrop-blur-sm border border-white/20 hover:border-white/40">
                                            Learn More
                                            <svg class="w-3 h-3 xl:w-4 xl:h-4 ml-1.5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"></path>
                                            </svg>
                                        </a>
                                    </div>
                                </div>
                            </div>
                        </div>

                        <!-- Research Slide 3: LLM & Human Cognition -->
                        <div class="research-slide w-full flex-shrink-0">
                            <div class="relative h-64 xl:h-96 bg-black/10 rounded-xl overflow-hidden backdrop-blur-sm">
                                <div class="absolute inset-0 flex items-center justify-center">
                                    <div class="text-center text-white px-4 xl:px-8">
                                        <div class="w-12 h-12 xl:w-16 xl:h-16 bg-white/20 rounded-full flex items-center justify-center mb-4 xl:mb-6 mx-auto">
                                            <svg class="w-6 h-6 xl:w-8 xl:h-8" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 14l9-5-9-5-9 5 9 5zm0 0v6m-3 0h6"></path>
                                            </svg>
                                        </div>
                                        <h3 class="text-lg xl:text-2xl font-serif font-bold mb-2 xl:mb-4 drop-shadow-lg">LLM and Human Cognitive Mechanisms</h3>
                                        <p class="text-sm xl:text-base opacity-90 max-w-sm xl:max-w-lg mx-auto drop-shadow-md mb-3 xl:mb-4">Aligning LLM agents with human cognition—preferences, personality, goals, and constraints for behavioral realism.</p>
                                        <a href="/publications" class="inline-flex items-center px-3 xl:px-4 py-1.5 xl:py-2 bg-white/20 hover:bg-white/30 text-white rounded-lg transition-all duration-300 font-medium text-xs xl:text-sm backdrop-blur-sm border border-white/20 hover:border-white/40">
                                            Learn More
                                            <svg class="w-3 h-3 xl:w-4 xl:h-4 ml-1.5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"></path>
                                            </svg>
                                        </a>
                                    </div>
                                </div>
                            </div>
                        </div>

                        <!-- Research Slide 4: Environmental Exposure & Health Effects -->
                        <div class="research-slide w-full flex-shrink-0">
                            <div class="relative h-64 xl:h-96 bg-black/10 rounded-xl overflow-hidden backdrop-blur-sm">
                                <div class="absolute inset-0 flex items-center justify-center">
                                    <div class="text-center text-white px-4 xl:px-8">
                                        <div class="w-12 h-12 xl:w-16 xl:h-16 bg-white/20 rounded-full flex items-center justify-center mb-4 xl:mb-6 mx-auto">
                                            <svg class="w-6 h-6 xl:w-8 xl:h-8" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8c-1.657 0-3 1.79-3 4s1.343 4 3 4 3-1.79 3-4-1.343-4-3-4zM5 20h14"></path>
                                            </svg>
                                        </div>
                                        <h3 class="text-lg xl:text-2xl font-serif font-bold mb-2 xl:mb-4 drop-shadow-lg">Environmental Exposure & Health Effects</h3>
                                        <p class="text-sm xl:text-base opacity-90 max-w-sm xl:max-w-lg mx-auto drop-shadow-md mb-3 xl:mb-4">Quantifying on-the-move exposure to air pollution, noise, heat, and greenspace with trajectory-level assessment.</p>
                                        <a href="/publications" class="inline-flex items-center px-3 xl:px-4 py-1.5 xl:py-2 bg-white/20 hover:bg-white/30 text-white rounded-lg transition-all duration-300 font-medium text-xs xl:text-sm backdrop-blur-sm border border-white/20 hover:border-white/40">
                                            Learn More
                                            <svg class="w-3 h-3 xl:w-4 xl:h-4 ml-1.5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"></path>
                                            </svg>
                                        </a>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                    
                    <!-- Carousel Navigation -->
                    <div class="flex justify-center mt-6 xl:mt-8 space-x-2">
                        <button class="carousel-dot w-2.5 h-2.5 xl:w-3 xl:h-3 rounded-full bg-primary transition-opacity" data-slide="0"></button>
                        <button class="carousel-dot w-2.5 h-2.5 xl:w-3 xl:h-3 rounded-full bg-gray-300 hover:bg-gray-400 transition-opacity" data-slide="1"></button>
                        <button class="carousel-dot w-2.5 h-2.5 xl:w-3 xl:h-3 rounded-full bg-gray-300 hover:bg-gray-400 transition-opacity" data-slide="2"></button>
                        <button class="carousel-dot w-2.5 h-2.5 xl:w-3 xl:h-3 rounded-full bg-gray-300 hover:bg-gray-400 transition-opacity" data-slide="3"></button>
                        <button class="carousel-dot w-2.5 h-2.5 xl:w-3 xl:h-3 rounded-full bg-gray-300 hover:bg-gray-400 transition-opacity" data-slide="4"></button>
                    </div>
                    
                    <!-- Navigation Arrows -->
                    <button class="carousel-prev absolute left-2 xl:left-4 top-1/2 -translate-y-1/2 w-10 h-10 xl:w-12 xl:h-12 bg-white/90 rounded-full shadow-lg hover:bg-white transition-colors flex items-center justify-center z-20">
                        <svg class="w-5 h-5 xl:w-6 xl:h-6 text-gray-700" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7"></path>
                        </svg>
                    </button>
                    <button class="carousel-next absolute right-2 xl:right-4 top-1/2 -translate-y-1/2 w-10 h-10 xl:w-12 xl:h-12 bg-white/90 rounded-full shadow-lg hover:bg-white transition-colors flex items-center justify-center z-20">
                        <svg class="w-5 h-5 xl:w-6 xl:h-6 text-gray-700" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"></path>
                        </svg>
                    </button>
                </div>
            </div>
        </section>
        </div>

        <!-- Latest News & Updates -->
        <section class="mb-12 lg:mb-16">
            <div class="text-center mb-8 lg:mb-12">
                <h2 class="text-3xl lg:text-4xl font-serif font-bold text-primary mb-4 lg:mb-6">Latest News & Updates</h2>

            </div>
            
            <!-- News Quick View Grid -->
            <div class={`grid gap-8 ${latestNews.length === 1 ? 'max-w-md mx-auto' : latestNews.length === 2 ? 'grid-cols-1 md:grid-cols-2 max-w-4xl mx-auto' : 'md:grid-cols-2 lg:grid-cols-3'}`}>
                {latestNews.map((news, index) => (
                    <article class="bg-white rounded-xl shadow-sm border border-gray-200 overflow-hidden hover:shadow-lg transition-all duration-300 group">
                        {news.data.image && (
                            <div class="aspect-video overflow-hidden">
                                <img 
                                    src={news.data.image} 
                                    alt={news.data.title}
                                    class="w-full h-full object-cover group-hover:scale-105 transition-transform duration-300"
                                />
                            </div>
                        )}
                        
                        <div class="p-6">
                            <div class="flex items-center gap-3 mb-4">
                                <span class="inline-block px-3 py-1 text-xs font-medium bg-primary/10 text-primary rounded-full">
                                    {news.data.category}
                                </span>
                                <time class="text-sm text-gray-500">
                                    {new Date(news.data.date).toLocaleDateString('en-US', {
                                        year: 'numeric',
                                        month: 'short',
                                        day: 'numeric'
                                    })}
                                </time>
                            </div>
                            
                            <h3 class="text-xl font-serif font-bold mb-3 text-gray-900 group-hover:text-primary transition-colors">
                                {news.data.title}
                            </h3>
                            
                            <p class="text-gray-600 mb-4 line-clamp-3">
                                {news.data.summary}
                            </p>
                            
                            <div class="flex items-center justify-between">
                                {news.data.author && (
                                    <span class="text-sm text-gray-500">
                                        by {news.data.author}
                                    </span>
                                )}
                            </div>
                        </div>
                    </article>
                ))}
            </div>
            
            <!-- View All News Archive Link -->
            <div class="text-center mt-8">
                <p class="text-gray-500 mb-4">
                    All news updates are displayed here as quick reports
                </p>
            </div>
        </section>

        <!-- Call to Action -->
        <section class="mb-12 lg:mb-16">
            <div class="bg-gradient-to-br from-gray-50 via-white to-blue-50 rounded-2xl lg:rounded-3xl p-8 lg:p-16 text-center border border-gray-100 shadow-xl">
                <h2 class="text-2xl lg:text-3xl xl:text-4xl font-serif font-bold text-primary mb-4 lg:mb-6"> Get to Know Us</h2>
                <div class="flex flex-row gap-4 lg:gap-6 justify-center max-w-2xl mx-auto">
                    <a 
                        href="/people" 
                        class="flex-1 inline-flex items-center justify-center px-6 lg:px-8 py-3 lg:py-4 border-2 border-primary text-primary rounded-xl hover:bg-primary hover:text-white transition-all duration-300 font-semibold text-base lg:text-lg shadow-lg hover:shadow-xl transform hover:-translate-y-1"
                    >
                        Meet Our Lab Members
                        <svg class="w-5 h-5 ml-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 4.354a4 4 0 110 5.292M15 21H3v-1a6 6 0 0112 0v1zm0 0h6v-1a6 6 0 00-9-5.197m13.5-9a2.5 2.5 0 11-5 0 2.5 2.5 0 015 0z"></path>
                        </svg>
                    </a>
                    <a 
                        href="/openings" 
                        class="flex-1 inline-flex items-center justify-center px-6 lg:px-8 py-3 lg:py-4 bg-primary text-white rounded-xl hover:bg-blue-800 transition-all duration-300 font-semibold text-base lg:text-lg shadow-lg hover:shadow-xl transform hover:-translate-y-1"
                    >
                        View Open Positions
                        <svg class="w-5 h-5 ml-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 13.255A23.931 23.931 0 0112 15c-3.183 0-6.22-.62-9-1.745M16 6V4a2 2 0 00-2-2h-4a2 2 0 00-2-2v2m8 0V6a2 2 0 012 2v6M8 6a2 2 0 012-2h4a2 2 0 012 2v6a2 2 0 01-2 2H10a2 2 0 01-2-2V6z"></path>
                        </svg>
                    </a>

                </div>
            </div>
        </section>
    </div>
</Layout>

<style>
    .line-clamp-2 {
        display: -webkit-box;
        -webkit-line-clamp: 2;
        -webkit-box-orient: vertical;
        overflow: hidden;
    }
    
    .line-clamp-3 {
        display: -webkit-box;
        -webkit-line-clamp: 3;
        -webkit-box-orient: vertical;
        overflow: hidden;
    }
    
    .research-carousel {
        transition: transform 0.5s ease-in-out;
        display: flex;
    }
    
    .research-slide {
        flex: 0 0 100%;
        width: 100%;
    }
    
    /* Enhanced shadow utilities */
    .shadow-3xl {
        box-shadow: 0 35px 60px -12px rgba(0, 0, 0, 0.25);
    }
    
    /* Hero section background animations */
    @keyframes float {
        0%, 100% {
            transform: translateY(0px) rotate(0deg);
            opacity: 0.1;
        }
        50% {
            transform: translateY(-10px) rotate(180deg);
            opacity: 0.2;
        }
    }
    
    @keyframes pulse-slow {
        0%, 100% {
            opacity: 0.05;
            transform: scale(1);
        }
        50% {
            opacity: 0.15;
            transform: scale(1.1);
        }
    }
    
    /* 为背景元素添加微妙的动画 */
    .absolute.inset-0.opacity-10 svg {
        animation: pulse-slow 8s ease-in-out infinite;
    }
    
    /* 浮动元素的自定义动画 */
    .animate-pulse {
        animation: pulse 2s cubic-bezier(0.4, 0, 0.6, 1) infinite;
    }
    
    /* Hero区域的额外视觉效果 */
    .hero-pattern::before {
        content: '';
        position: absolute;
        top: 0;
        left: 0;
        right: 0;
        bottom: 0;
        background-image: 
            radial-gradient(circle at 20% 80%, rgba(255,255,255,0.05) 2px, transparent 2px),
            radial-gradient(circle at 80% 20%, rgba(255,255,255,0.05) 2px, transparent 2px),
            radial-gradient(circle at 40% 40%, rgba(255,255,255,0.03) 2px, transparent 2px);
        background-size: 50px 50px, 60px 60px, 40px 40px;
        animation: float 20s ease-in-out infinite;
    }
    
    /* Smooth transitions for interactive elements */
    .transform {
        transition: transform 0.3s ease-in-out;
    }
    
    /* Enhanced button hover effects */
    .hover\:-translate-y-1:hover {
        transform: translateY(-0.25rem);
    }
    
    /* Carousel navigation buttons */
    .carousel-prev, .carousel-next, .news-prev, .news-next {
        cursor: pointer;
        user-select: none;
        pointer-events: auto;
    }
    
    .carousel-prev:active, .carousel-next:active, .news-prev:active, .news-next:active {
        transform: translateY(-50%) scale(0.95);
    }
</style>

<script>
    document.addEventListener('DOMContentLoaded', function() {
        // Research Carousel - Simple Implementation
        const carousel = document.querySelector('.research-carousel') as HTMLElement;
        const slides = document.querySelectorAll('.research-slide');
        const dots = document.querySelectorAll('.carousel-dot');
        const prevButton = document.querySelector('.carousel-prev');
        const nextButton = document.querySelector('.carousel-next');
        
        let currentSlide = 0;
        let autoPlayInterval: number;
        const totalSlides = slides.length;
        
        function goToSlide(slideIndex: number) {
            currentSlide = slideIndex;
            if (carousel) {
                carousel.style.transform = `translateX(-${currentSlide * 100}%)`;
            }
            
            // Update dots
            dots.forEach((dot, index) => {
                if (index === currentSlide) {
                    dot.classList.remove('bg-gray-300', 'hover:bg-gray-400');
                    dot.classList.add('bg-primary');
                } else {
                    dot.classList.remove('bg-primary');
                    dot.classList.add('bg-gray-300', 'hover:bg-gray-400');
                }
            });
        }
        
        function nextSlide() {
            currentSlide = (currentSlide + 1) % totalSlides;
            goToSlide(currentSlide);
        }
        
        function prevSlide() {
            currentSlide = (currentSlide - 1 + totalSlides) % totalSlides;
            goToSlide(currentSlide);
        }
        
        function startAutoPlay() {
            autoPlayInterval = window.setInterval(nextSlide, 5000);
        }
        
        function stopAutoPlay() {
            if (autoPlayInterval) {
                clearInterval(autoPlayInterval);
            }
        }
        
        // Initialize first slide
        goToSlide(0);
        
        // Event listeners for research carousel
        dots.forEach((dot, index) => {
            dot.addEventListener('click', () => {
                goToSlide(index);
                stopAutoPlay();
                startAutoPlay();
            });
        });
        
        if (nextButton) {
            nextButton.addEventListener('click', () => {
                nextSlide();
                stopAutoPlay();
                startAutoPlay();
            });
        }
        
        if (prevButton) {
            prevButton.addEventListener('click', () => {
                prevSlide();
                stopAutoPlay();
                startAutoPlay();
            });
        }
        
        // Pause autoplay on hover
        const carouselContainer = carousel?.parentElement?.parentElement;
        if (carouselContainer) {
            carouselContainer.addEventListener('mouseenter', stopAutoPlay);
            carouselContainer.addEventListener('mouseleave', startAutoPlay);
        }
        
        // Touch/swipe support for research carousel
        let touchStartX = 0;
        let touchEndX = 0;
        
        if (carousel) {
            carousel.addEventListener('touchstart', (e: TouchEvent) => {
                touchStartX = e.changedTouches[0].screenX;
            });
            
            carousel.addEventListener('touchend', (e: TouchEvent) => {
                touchEndX = e.changedTouches[0].screenX;
                handleSwipe();
            });
        }
        
        function handleSwipe() {
            const swipeThreshold = 50;
            const diff = touchStartX - touchEndX;
            
            if (Math.abs(diff) > swipeThreshold) {
                if (diff > 0) {
                    nextSlide();
                } else {
                    prevSlide();
                }
                stopAutoPlay();
                startAutoPlay();
            }
        }
        
        // Start autoplay
        startAutoPlay();
        
        // Keyboard navigation
        document.addEventListener('keydown', (e) => {
            if (e.key === 'ArrowLeft') {
                prevSlide();
                stopAutoPlay();
                startAutoPlay();
            } else if (e.key === 'ArrowRight') {
                nextSlide();
                stopAutoPlay();
                startAutoPlay();
            }
        });
    });
</script>
